services:
  nodejs-server:
    image: dubeyjags/excloud
    deploy:
      replicas: 5
    networks:
      - nodeServer-internal
  caddy:
    image: caddy:latest
    ports:
      - 80:80
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    networks:
      - nodeServer-internal

networks:
  nodeServer-internal:
    driver: bridge
